{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "windows-window-controls",
  "description": "Windows-only permissions for the custom frameless window controls.",
  "windows": ["main"],
  "platforms": ["windows"],
  "permissions": ["core:window:allow-destroy"]
}
