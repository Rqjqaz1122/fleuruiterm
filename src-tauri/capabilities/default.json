{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Minimum permissions for the FleurTerm main window",
  "windows": ["main"],
  "permissions": ["core:default"]
}
